#COMANDO WHILE

PROGRAM DIVISION.
MAIN.
PIC9 X VALUE 0.
PIC9 Y VALUE 0.
PICBOOL verdade VALUE TRUE.

PERFORM UNTIL (X < 100).
	DISPLAY X.
	ACCEPT X FROM COMPUTE(X+1).
	IF (X=77) THEN
	BREAK.
	ENDIF
	IF (X>50) THEN 
	CONTINUE.
	ENDIF	
	ACCEPT Y FROM COMPUTE(Y-1).

ENDPERFORM
DISPLAY 1111111.
DISPLAY Y.
DISPLAY X.
ENDMAIN