GLOBALDATA DIVISION.

PROGRAM DIVISION.

FIBO VOID USING PIC9 X.
PIC9 AUX.
PIC9 CONTADOR VALUE 0.
PIC9 A VALUE 0.
PIC9 B VALUE 0.

PERFORM UNTIL (CONTADOR < X).

ACCEPT AUX FROM COMPUTE(A + B).
ACCEPT A FROM B.
ACCEPT B FROM AUX.

ACCEPT CONTADOR FROM COMPUTE(CONTADOR + 1).

DISPLAY AUX.
DISPLAY CONTADOR.
ENDPERFORM

ENDFUNCTION




MAIN.
PIC9 X VALUE 8.
CALL FIBO USING X.
ENDMAIN

