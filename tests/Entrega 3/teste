GLOBALDATA DIVISION.
	#PICBOOL var1 VALUE TRUE.
	#PICBOOL var2 VALUE FALSE.
	#PIC9 var3 VALUE 10.
	PIC9 var4 VALUE (-1).

PROGRAM DIVISION.

MAIN.
DISPLAY COMPUTE(16/(4*(-2))).
	IF ((-3)>(+2)) THEN
		DISPLAY TRUE.
	ELSE
		DISPLAY FALSE.
	ENDIF
ENDMAIN